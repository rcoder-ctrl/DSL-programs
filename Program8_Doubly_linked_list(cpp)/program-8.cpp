/*Write C++ program for storing binary number using doubly linked lists. Write functions-
a) To compute 1‘s and 2‘s complement
b) Add two binary numbers*/

struct Node{
    int number;
    struct Node *next;
    struct Node *prev;
};

class binary{
    public:
    struct Node *num1,*num2,*num3,*num4,*carry;
    void get_data(){
        
    }
};